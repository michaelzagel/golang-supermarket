apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: inventory-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: inventory.golang-supermarket.com
    http:
      paths:
      - backend:
          serviceName: inventory-service
          servicePort: 80
    