apiVersion: v1
kind: Service
metadata:
  name: delivery-service
  labels:
    app: delivery
spec:
  type: ClusterIP
  selector:
    app: delivery
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
